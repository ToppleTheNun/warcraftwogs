// Single player cannot cast Word of Glory more than once every 750ms.
export const MINIMUM_GCD_MS = 750;

// Different report tolerance.
export const DIFFERENT_REPORT_TOLERANCE = 2000;

// How different a single heal can be to trigger an update.
export const HEAL_AMOUNT_TOLERANCE = 100;
